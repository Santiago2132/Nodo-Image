# Ultra-optimized dependencies for massive image processing
fastapi==0.104.1
uvicorn[standard]==0.24.0
uvloop==0.19.0;sys_platform!="win32"  # Unix/Linux only
httptools==0.6.1;sys_platform!="win32"  # Unix/Linux only
Pillow-SIMD==10.0.1.post1  # SIMD-optimized Pillow (fallback to regular Pillow)
numpy==1.24.3           # Vectorized operations
psutil==5.9.6           # System monitoring
python-multipart==0.0.6

# Windows fallbacks
Pillow==10.1.0          # Fallback if Pillow-SIMD not available

# Optional performance boosters
# orjson==3.9.10        # Faster JSON (if needed)
# cython==3.0.5         # Compile critical paths